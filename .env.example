# Local configuration for development/testing

# Grafana URL used by Playwright tests and tooling
GRAFANA_URL=http://127.0.0.1:3001

# Optional: credentials for plugin-e2e auth (defaults are admin/admin)
# GRAFANA_USERNAME=admin
# GRAFANA_PASSWORD=admin

# Datasource configuration (used by provisioning)
OPENWEATHER_API_KEY=replace_with_real_key
OPENWEATHER_BASE_URL=http://api.openweathermap.org/data/2.5/forecast
